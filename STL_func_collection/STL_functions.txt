sort
(시작반복자, 끝반복자, 정렬방법(함수))
시작반복자부터 끝 반복자까지 연속된 메모리를 정렬방법을 통해 정렬
-
sort
(시작반복자, 끝반복자)
시작반복자부터 끝 반복자까지 오름차순 정렬
-
for_each
(시작반복자, 끝반복자, 함수)
반복자 범위 내 모든 요소에 대해 함수 실행 /	병렬 처리를 위한 오버로드 존재
-
equal_range
(시작반복자, 끝반복자, 찾는 값)
정렬된 메모리에 사용해야 하며 찾는 값들이 존재하면 범위를 두 개의 반복자로 반환
iterator
equal_range
(시작반복자, 끝반복자, 찾는 값, 함수)
정렬된 메모리에 사용해야 하며 함수를 만족하는 값들이 존재하면 범위를 두 개의 반복자로 반환
iterator
count
(시작반복자, 끝반복자, 찾는 값)
반복자 범위 내 모든 요소에 대해 찾는 값과 같은 값의 개수를 반환
value
count_if
(시작반복자, 끝반복자, 함수)
반복자 범위 내 모든 요소에 대해 함수를 만족하는 요소의 개수를 반환
value
accumulate
(시작반복자, 끝반복자, 초기 값)
반복자 범위 내 모든 요소를 모두 더한 후 반환한다. 시작 값은 초기값이다.
value
accumulate
(시작반복자, 끝반복자, 초기 값, 사용자 정의 연산)
반복자 범위 내 모든 요소를 사용자 정의 연산을 이용해 계산하여 반환한다. 곱셉을 정의하면 모두 곱셈하여 반환
value
find
(시작반복자, 끝반복자, 찾는 값)
반복자 범위 내에서 찾는 값을 처음 만나면 그 값 위치의 반복자를 반환, 찾지 못하면 끝반복자 반환
iterator
find_if
(시작반복자, 끝반복자, 함수)
반복자 범위 내에서 함수의 조건을 만족하는 값을 처음 만나면 그 값 위치의 반복자를 반환, 찾지 못하면 끝반복자 반환
iterator
reverse
(시작반복자, 끝반복자)
반복자 범위 내 모든 요소의 순서를 뒤집는다.
-
lower_bound
(시작반복자, 끝반복자, 찾는 값)
정렬된 메모리에 사용해야 하며 찾는 값 이상의 첫 요소에 대한 반복자 반환, 못찾으면 끝반복자 반환
iterator
upper_bound
(시작반복자, 끝반복자, 찾는 값)
정렬된 메모리에 사용해야 하며 찾는 값을 초과하는 첫 요소에 대한 반복자 반환, 못찾으면 끝반복자 반환
iterator
transform
(시작반복자, 끝반복자, 저장할 메모리 시작반복자, 연산함수)
반복자 범위 내에 연산함수를 적용해 저장할 메모리의 시작반복자부터 결과를 차례로 저장
-
transform
(시작반복자, 끝반복자, 다른 메모리 시작반복자, 저장할 메모리 시작반복자, 연산함수)
반복자 범위 내에 연산함수를 적용해 다른 두 메모리의 연산 결과를 저장할 메모리의 시작반복자부터 차례로 저장
-
copy
(시작반복자, 끝반복자, 복사할 메모리의 시작반복자)
반복자 범위 내의 요소를 복사하여 복사할 메모리의 시작반복자부터 저장
-
remove
(시작반복자, 끝반복자, 제거할 값)
제거할 값을 실제로 제거하지 않고 범위 끝으로 이동시킨 후 새로운 끝을 가리키는 반복자를 반환하며 제거를 위해서는 erase함수와 같이 사용해야 한다.
iterator
remove_if
(시작반복자, 끝반복자, 함수)
함수를 만족하는 값을 실제로 제거하지 않고 범위 끝으로 이동시킨 후 새로운 끝을 가리키는 반복자를 반환하며 제거를 위해서는 erase함수와 같이 사용해야 한다.
iterator
erase
(시작반복자, 끝반복자)
반복자 범위 내 모든 요소를 제거하고 remove함수와 같이 사용할경우 remove 함수는 시작반복자에 위치하면 된다. 반환값은 제거된 요소들의 바로 다음 요소의 반복자
iterator
erase
(제거할 위치의 반복자)
반복자가 가리키는 요소를 제거하고 제거한 다음 요소를 가리키는 반복자 반환
iterator
search
(시작반복자, 끝반복자, 서브 메모리 시작반복자, 서브 메모리 끝반복자)
반복자 범위 내에 서브 메모리의 시작-끝 반복자에 해당하는 모든 요소가 일치하는 부분이 있다면 그 부분의 첫 요소에 대한 반복자 반환
iterator
fill
(시작반복자, 끝반복자, 채울 값)
반복자 범위 내 모든 요소를 특정 값으로 채운다
-
fill_n
(시작반복자, 채울 요소 수, 채울 값)
시작반복자가 가리키는부터 채울 요소 수만큼을 특정 값으로 채운다
-
unique
(시작반복자, 끝반복자)
정렬된 메모리의 반복자 범위 내 중복되는 요소를 모두 뒤로 옮긴 후 새로운 끝반복자를 반환하며 완전 제거를 위해서는 erase 함수 추가사용
iterator
unique
(시작반복자, 끝반복자, 함수)
정렬된 메모리의 반복자 범위 내 이진조건함수를 만족하는 중복요소를 모두 뒤로 옮긴 후 새로운 끝반복자를 반환하며 완전 제거를 위해서는 erase 함수 추가사용
iterator
binary_search
(시작반복자, 끝반복자, 찾는 값)
정렬된 메모리의 반복자 범위 내 찾는 값을 이진 검색하여 bool값 반환
bool